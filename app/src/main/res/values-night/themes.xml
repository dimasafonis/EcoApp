<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.EcoApp" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">#099023</item>
        <item name="colorOnPrimary">@android:color/white</item>

        <item name="android:background">#404040</item>
        <item name="android:textColor">@android:color/white</item>
        <item name="settingsColor">#004D40</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">#099023</item>
        <!-- Customize your theme here. -->
    </style>
</resources>