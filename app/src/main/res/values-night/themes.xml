<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.EcoApp" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">#099023</item>
        <item name="colorOnPrimary">@android:color/white</item>

        <item name="android:background">@color/night_background</item>
        <item name="android:textColor">@android:color/white</item>
        <item name="settingsColor">#08781F</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor">#099023</item>
        <!-- Customize your theme here. -->
    </style>
</resources>